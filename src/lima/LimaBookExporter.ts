import{LimaSerializer}from"./LimaSerializer";import{LimaDocumentRepository}from"../repositories/LimaDocumentRepository";export class LimaBookExporter{public constructor(private readonly repository:LimaDocumentRepository){}public async exportLimaBook(bookId:string):Promise<File>{const document=await this.repository.get(bookId);if(!document)throw new Error("Este livro ainda não possui conversão LIMA.");const bytes=new LimaSerializer().serialize(document),name=this.slug(document.metadata.title);return new File([new Uint8Array(bytes).buffer],`${name}.lima`,{type:"application/x-lima-book"});}private slug(value:string):string{return value.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLocaleLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")||"livro";}}
