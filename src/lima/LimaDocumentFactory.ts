import{LimaDocument,type LimaAsset,type LimaBlock,type LimaChapter,type LimaSourceFormat}from"./LimaDocument";import type{LimaConversionInput}from"./LimaConverter";import{LimaValidator}from"./LimaValidator";
export class LimaDocumentFactory{public create(input:LimaConversionInput,sourceFormat:LimaSourceFormat,blocks:LimaBlock[],chapters:LimaChapter[],assets:LimaAsset[]=[]):LimaDocument{const createdAt=new Date().toISOString(),coverAsset=this.cover(input.cover);if(coverAsset)assets.unshift(coverAsset);const document=new LimaDocument({format:"lima",version:1,documentId:input.id,createdAt,generator:"Lumeo LimaConverter v1",sourceFormat,capabilities:{reflow:true,images:assets.some(asset=>asset.type==="image"),chapters:chapters.length>0,highlights:true,annotations:true,search:true}},{id:input.id,title:input.title,author:input.author,language:input.language??"pt-BR",genre:input.genre??"",collection:input.collection,coverAssetId:coverAsset?.id,sourceFormat,createdAt},chapters,blocks,assets,chapters.map(chapter=>({id:`nav-${chapter.id}`,title:chapter.title,chapterId:chapter.id,blockId:chapter.startBlockId,order:chapter.order})));new LimaValidator().validate(document);return document;}private cover(value:string|undefined):LimaAsset|null{if(!value)return null;const match=value.match(/^data:([^;,]+);base64,(.+)$/);if(!match?.[1]||!match[2])return{id:"cover",type:"cover",mimeType:"text/uri-list",localReference:value};const binary=atob(match[2]),data=Uint8Array.from(binary,char=>char.charCodeAt(0));return{id:"cover",type:"cover",mimeType:match[1],data};}}
