import type{LimaAnchor,LimaBlock,LimaChapter}from"../../lima/LimaDocument";export type ChapterProgressStatus="not-started"|"in-progress"|"completed";export interface ChapterProgress{percent:number;status:ChapterProgressStatus;}
export class ChapterProgressService{public calculate(chapter:LimaChapter,blocks:readonly LimaBlock[],anchor:LimaAnchor):ChapterProgress{const chapterBlocks=blocks.filter(block=>block.chapterId===chapter.id).sort((a,b)=>a.order-b.order),current=blocks.find(block=>block.id===anchor.blockId);if(!current||!chapterBlocks.length)return{percent:0,status:"not-started"};const first=chapterBlocks[0]!.order,last=chapterBlocks.at(-1)!.order;if(current.order<first)return{percent:0,status:"not-started"};if(current.order>last||current.id===chapter.endBlockId&&anchor.offset>=current.content.length)return{percent:100,status:"completed"};const blockIndex=chapterBlocks.findIndex(block=>block.id===current.id),within=current.content.length?Math.min(1,anchor.offset/current.content.length):0,percent=Math.round(((blockIndex+within)/chapterBlocks.length)*100);return{percent:Math.max(1,Math.min(99,percent)),status:"in-progress"};}}
