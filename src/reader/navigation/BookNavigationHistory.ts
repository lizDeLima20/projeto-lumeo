import type{LimaAnchor}from"../../lima/LimaDocument";export class BookNavigationHistory{private readonly stack:LimaAnchor[]=[];public push(anchor:LimaAnchor):void{const last=this.stack.at(-1);if(!last||last.blockId!==anchor.blockId||last.offset!==anchor.offset)this.stack.push({...anchor});}public back():LimaAnchor|null{return this.stack.pop()??null;}public get canGoBack():boolean{return this.stack.length>0;}public clear():void{this.stack.length=0;}}
