import{PageGeometry,type PageTransform}from"./PageGeometry";import{PageGestureController}from"./PageGestureController";import{PageShadowRenderer}from"./PageShadowRenderer";
export type PageTurnState="IDLE"|"DRAGGING"|"COMPLETING"|"RETURNING"|"DISABLED";export type TurnDirection=1|-1;
export class PageTurnEngine {private stateValue:PageTurnState="IDLE";private readonly gesture=new PageGestureController();private frame=0;private pending:PageTransform|null=null;private velocity=0;private direction:TurnDirection=1;public constructor(private readonly page:HTMLElement,private readonly under:HTMLElement|null,private readonly commit:(direction:TurnDirection)=>void,private readonly geometry=new PageGeometry(),private readonly shadows=new PageShadowRenderer(),private readonly threshold=.35){}
public get state():PageTurnState{return this.stateValue;}public begin(x:number,time=performance.now()):boolean{if(this.stateValue!=="IDLE")return false;this.stateValue="DRAGGING";this.gesture.start(x,time);this.page.classList.add("page-turn-active");this.page.style.willChange="transform";return true;}public move(x:number,time=performance.now()):number{if(this.stateValue!=="DRAGGING")return 0;const sample=this.gesture.update(x,time);this.direction=sample.direction;this.velocity=sample.velocityX;this.pending=this.geometry.calculate(sample.deltaX,this.page.clientWidth,this.direction);this.schedule();return this.pending.progress;}public async end(x:number,time=performance.now()):Promise<boolean>{if(this.stateValue!=="DRAGGING")return false;const sample=this.gesture.finish(x,time);this.direction=sample.direction;this.velocity=sample.velocityX;const transform=this.geometry.calculate(sample.deltaX,this.page.clientWidth,this.direction);this.apply(transform);const complete=this.shouldComplete(transform.progress,this.velocity,this.direction);await this.settle(complete,transform);if(complete)this.commit(this.direction);return complete;}public cancel():Promise<void>{if(this.stateValue!=="DRAGGING")return Promise.resolve();const current=this.pending??this.geometry.calculate(0,this.page.clientWidth,this.direction);return this.settle(false,current).then(()=>undefined);}public shouldComplete(progress:number,velocityX:number,direction:TurnDirection):boolean{return progress>=this.threshold||Math.abs(velocityX)>=.55&&(direction===1?velocityX<0:velocityX>0);}public async programmatic(direction:TurnDirection):Promise<boolean>{if(!this.begin(direction===1?this.page.clientWidth:0))return false;this.direction=direction;const start=this.geometry.calculate(this.page.clientWidth*.08,this.page.clientWidth,direction);this.apply(start);await this.settle(true,start);this.commit(direction);return true;}public disable():void{this.stateValue="DISABLED";}
private schedule():void{if(this.frame)return;this.frame=requestAnimationFrame(()=>{this.frame=0;if(this.pending)this.apply(this.pending)});}private transform(value:PageTransform):string{return`translateX(${value.translateX}px) translateZ(${value.translateZ}px) rotateY(${value.angle}deg) skewY(${value.skewY}deg) scaleX(${value.scaleX})`;}private apply(value:PageTransform):void{this.page.style.transformOrigin=value.origin;this.page.style.transform=this.transform(value);this.shadows.render(this.page,this.under,value);}private async settle(complete:boolean,current:PageTransform):Promise<void>{this.stateValue=complete?"COMPLETING":"RETURNING";cancelAnimationFrame(this.frame);this.frame=0;const target=this.geometry.calculate(complete?this.page.clientWidth:0,this.page.clientWidth,this.direction);const animation=this.page.animate([{transform:this.page.style.transform},{transform:this.transform(target)}],{duration:Math.max(180,(complete?1-current.progress:current.progress)*420),easing:"cubic-bezier(.18,.72,.2,1)",fill:"forwards"});await animation.finished.catch(()=>undefined);this.page.style.transform="";this.page.style.transformOrigin="";this.page.style.willChange="";this.page.classList.remove("page-turn-active");this.shadows.clear(this.page,this.under);this.stateValue="IDLE";}
}
