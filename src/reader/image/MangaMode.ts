export type MangaReadingDirection="ltr"|"rtl";export interface MangaPreferences{enabled:boolean;direction:MangaReadingDirection;pageLayout:"single"|"double";}
export class MangaMode{public constructor(private readonly preferences:MangaPreferences){}public next(current:number,total:number):number{const delta=this.preferences.direction==="rtl"?-1:1;return Math.min(total,Math.max(1,current+delta));}public previous(current:number,total:number):number{const delta=this.preferences.direction==="rtl"?1:-1;return Math.min(total,Math.max(1,current+delta));}public layout(width:number):"single"|"double"{return width<768?"single":this.preferences.pageLayout;}}
