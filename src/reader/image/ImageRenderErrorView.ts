import{I18nManager}from"../../i18n/I18nManager";
export class ImageRenderErrorView{public constructor(private readonly retry:()=>void,private readonly previous:()=>void,private readonly next:()=>void,private readonly i18n=I18nManager.shared){}public render():HTMLElement{const wrapper=document.createElement("div");wrapper.className="reader-image-error";wrapper.setAttribute("role","alert");const message=document.createElement("p");message.textContent=this.i18n.t("reader.imageMode.renderError");wrapper.append(message,this.button(this.i18n.t("lookup.retry"),this.retry),this.button(this.i18n.t("reader.previousPage"),this.previous),this.button(this.i18n.t("reader.nextPage"),this.next));return wrapper;}private button(text:string,action:()=>void):HTMLButtonElement{const button=document.createElement("button");button.type="button";button.textContent=text;button.addEventListener("click",action);return button;}}
