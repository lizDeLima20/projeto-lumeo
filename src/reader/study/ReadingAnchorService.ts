import type{HighlightData}from"../../models/Highlight";import type{ReaderPage,ReadingAnchor}from"../reflow/ReaderDocument";
export class ReadingAnchorService{public forHighlight(highlight:HighlightData,pages:readonly ReaderPage[]):ReadingAnchor{let logicalOffset=0;for(const page of pages){for(const block of page.paragraphs){if((block.sourceBlockId??block.id)===highlight.blockId)return{paragraphId:highlight.blockId,textOffset:highlight.startOffset,logicalOffset:page.startOffset+Math.max(0,highlight.startOffset-(block.sourceStart??0))};logicalOffset=Math.max(logicalOffset,page.endOffset);}}return{paragraphId:highlight.blockId,textOffset:highlight.startOffset,logicalOffset};}public pageFor(anchor:ReadingAnchor,pages:readonly ReaderPage[]):number{const index=pages.findIndex(page=>page.paragraphs.some(block=>(block.sourceBlockId??block.id)===anchor.paragraphId&&(block.sourceStart??0)<=anchor.textOffset&&(block.sourceStart??0)+block.text.length>=anchor.textOffset));return index<0?1:index+1;}}
