import type{HighlightColor}from"../../models/Highlight";import type{StudyEntry,StudyEntryType}from"./StudyNotebook";export class StudyFilterController{public filter(entries:readonly StudyEntry[],type:"all"|StudyEntryType="all",color?:HighlightColor,query=""):StudyEntry[]{const needle=this.normalize(query);return entries.filter(entry=>(type==="all"||entry.type===type)&&(!color||entry.highlightColor===color)&&(!needle||this.normalize(`${entry.selectedText} ${entry.noteText??""} ${entry.chapterTitle}`).includes(needle)));}private normalize(value:string):string{return value.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLocaleLowerCase().replace(/\s+/g," ").trim();}}
