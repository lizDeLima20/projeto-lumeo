export class OpenBookNavigationController {public constructor(private readonly total:number){}public spreadStart(page:number):number{if(page<=1)return 0;return page%2===0?page:page-1;}public next(page:number):number{return Math.min(this.total,this.spreadStart(page)+2);}public previous(page:number):number{return Math.max(1,this.spreadStart(page)-2);}public canNext(page:number):boolean{return this.spreadStart(page)+2<=this.total;}public canPrevious(page:number):boolean{return this.spreadStart(page)>0;}}
