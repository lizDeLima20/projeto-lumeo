import type{BookFocusLabelData}from"./BookContextLabelResolver";
import{I18nManager}from"../i18n/I18nManager";
export class BookFocusLabel {public render(data:BookFocusLabelData,onOpen:()=>void):HTMLButtonElement{const label=document.createElement("button");label.type="button";label.className="book-focus-label";const title=document.createElement("strong");title.textContent=data.title;label.append(title);if(data.subtitle){const subtitle=document.createElement("small");subtitle.textContent=data.subtitle;label.append(subtitle);}const progress=document.createElement("small");progress.className="book-focus-label__progress";progress.textContent=I18nManager.shared.t("ui.book.readingProgress",{percent:data.progressPercent});label.append(progress);label.addEventListener("click",onOpen);return label;} }
