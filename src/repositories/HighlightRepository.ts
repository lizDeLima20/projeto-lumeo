import type{HighlightData}from"../models/Highlight";import{IndexedDbService,STORE_NAMES}from"../services/IndexedDbService";
export class HighlightRepository{public constructor(private readonly database:IndexedDbService){}public save(value:HighlightData):Promise<void>{return this.database.request(STORE_NAMES.highlights,"readwrite",store=>store.put(value)).then(()=>undefined);}public async get(id:string):Promise<HighlightData|null>{return await this.database.request<HighlightData|undefined>(STORE_NAMES.highlights,"readonly",store=>store.get(id))??null;}public async byBook(bookId:string):Promise<HighlightData[]>{return this.database.request(STORE_NAMES.highlights,"readonly",store=>store.index("bookId").getAll(bookId));}public async byBlock(bookId:string,blockId:string):Promise<HighlightData[]>{return this.database.request(STORE_NAMES.highlights,"readonly",store=>store.index("bookBlock").getAll([bookId,blockId]));}public delete(id:string):Promise<void>{return this.database.request(STORE_NAMES.highlights,"readwrite",store=>store.delete(id)).then(()=>undefined);}public async deleteByBook(bookId:string):Promise<void>{const items=await this.byBook(bookId);await Promise.all(items.map(item=>this.delete(item.id)));}}
