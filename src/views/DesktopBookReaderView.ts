import{OpenBookLayout}from"../reader/desktop/OpenBookLayout";import{PageSpread}from"../reader/desktop/PageSpread";import{PageTurnInteractionController}from"../reader/desktop/PageTurnInteractionController";import type{ReaderPage,ReaderParagraph}from"../reader/reflow/ReaderDocument";
export class DesktopBookReaderView {private interaction:PageTurnInteractionController|null=null;public constructor(private readonly left:ReaderPage|null,private readonly right:ReaderPage|null,private readonly previous:()=>void,private readonly next:()=>void,private readonly paragraph?:(value:ReaderParagraph)=>HTMLElement){}public render():HTMLElement{const stage=document.createElement("div");stage.className="desktop-book-reader";const book=new OpenBookLayout(this.paragraph).render(new PageSpread(this.left,this.right));this.interaction=new PageTurnInteractionController(book,this.next,this.previous);this.interaction.bind();const prev=this.arrow("←","Página dupla anterior",()=>void this.turn(-1)),next=this.arrow("→","Próxima página dupla",()=>void this.turn(1));stage.append(prev,book,next);return stage;}public turn(direction:1|-1):Promise<boolean>{return this.interaction?.turn(direction)??Promise.resolve(false)}public destroy():void{this.interaction?.unbind();}private arrow(text:string,label:string,action:()=>void):HTMLButtonElement{const button=document.createElement("button");button.type="button";button.className="open-book-arrow";button.textContent=text;button.setAttribute("aria-label",label);button.addEventListener("click",action);return button;}}
